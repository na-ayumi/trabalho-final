datasource db {
  provider = "sqlite"
  url      = "file:./database/db.db"
}

generator client {
  provider = "prisma-client-js"
}

model Car {
  licensePlate String  @id
  available    Boolean
}

model Rental {
  id        String    @id
  carId     String
  startDate DateTime
  endDate   DateTime
  createAt  DateTime  @default(now())
}
